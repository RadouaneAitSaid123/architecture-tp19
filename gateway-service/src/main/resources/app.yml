spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: r1
              uri: http://localhost:8088/
              predicates:
                - Path=/client/**,/clients/**